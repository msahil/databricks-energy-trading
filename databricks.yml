# Declarative Automation Bundle (root):
# - Job `energy_trading_demo_data`: reset schema + short-term notebooks → UC Delta (serverless).
# - App `energy_trading_app`: Dash UI skeleton in app/ (app.yaml, requirements.txt).
#
# Deploy:  databricks bundle deploy
# Seed UC:  databricks bundle run energy_trading_demo_data
# App:     databricks bundle run energy_trading_app
#
bundle:
  name: databricks-energy-trading

include:
  - resources/*.yml

sync:
  exclude:
    - .git
    - "**/.databricks"
    - "**/.DS_Store"
    - app/.venv

targets:
  dev:
    default: true
    mode: development
    workspace:
      root_path: /Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      root_path: /Shared/.bundle/${bundle.name}/${bundle.target}
